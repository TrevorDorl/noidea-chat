#include <SDL.h>
#include <stdio.h>
//going to be used for testing creating windows
char* test()
{
	//SDL_Init(SDL_INIT_VIDEO);
	//SDL_Surface *window = SDL_SetVideoMode(640, 480, 32, SDL_SWSURFACE);
	char *test = "IMAWAT";
	return test;
	}
//initialize the video and return 0 on success, return 
